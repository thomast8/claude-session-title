{
  "description": "Stop hook that auto-generates a kebab-case session title via Haiku after the first assistant turn. Spawns a detached background worker (zero prompt latency, no timeout race) which writes the title to the transcript as a custom-title record. One-shot per session, recursion-safe, slugified-prompt fallback.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/session-title.sh",
            "timeout": 10000,
            "statusMessage": "Naming session..."
          }
        ]
      }
    ]
  }
}
